/**
 * Represents all of the possible values which a panel
 * might return in response to a query.
 * 
 * There is no standard for this, so most fields will
 * likely be nullable.
 */
export default interface IPanelQueryResult{
}